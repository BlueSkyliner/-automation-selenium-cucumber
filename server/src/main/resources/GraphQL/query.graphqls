schema {
    query: Query
    mutation: Mutation
}

type Query {
    FindUserId(id: ID!): user
    FindAllUser: [user]

    FindBoardId(id: ID!): WrappingBoard
    FindBoardIdAdmin(id: ID!): WrappingAdminBoard
    FindBoards(Type: SearchTypes, Li